

plugins {
    id("java")
}

group = "org.example"
version = "1.0-SNAPSHOT"


repositories {
    mavenCentral()
}

dependencies {
    testImplementation(platform("org.junit:junit-bom:5.10.0"))
    testImplementation("org.junit.jupiter:junit-jupiter")
    testImplementation("com.google.api.grpc:proto-google-common-protos:0.1.9")
    testImplementation("com.google.api:gax-grpc")
    testImplementation(platform("com.google.cloud:libraries-bom:26.54.0"))
    testImplementation("com.google.cloud:google-cloud-dialogflow-cx")
}

tasks.test {
    useJUnitPlatform()
}
